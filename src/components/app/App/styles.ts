import { SxStyles } from 'types/styles';

export const styles: SxStyles = {
  container: {
    display: 'flex',
    alignItems: 'flex-start',
    gap: '10px',
  },

  mapSection: {
    height: '100vh',
    width: '60%',
  },
};
